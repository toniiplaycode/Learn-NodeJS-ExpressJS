<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NodeJS AskIT</title> 
    <link rel="stylesheet" href="/css/style.css"> <!-- nhúng đường dẫn bắt đầu từ folder CSS, bên file viewEngine.js nó đã có đường dẫn từ ./src/public rồi, nên trong file index.ejs này mình chỉ dẫn thêm đường dẫn /css/style.css là đủ (ráp lại là ./src/public/css/style.css)-->
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
</head>
<body>
    <div class="app">
        <div class="topnav">
            <a href="/" class="active">Home</a>
            <a href="/upload">Upload Files</a>
          </div>
    
          <h2 class="title"><%= testVariable%></h2>
          <!-- nhúng đường dẫn ảnh từ folder images -->
          <img src="/images/imageStaticFile.png" alt="image static file">
    
          <div>
            <h2>List Users:</h2>
            <!-- in ra biến được truyền tham số thứ 2 của res.render(...), ban đầu nó dạng object của JS nên không render ra được, phải chuyển thành dạng chuỗi mới render ra HTML được -->
            <!-- <%= JSON.stringify(dataUsers) %> -->
    
            <table class="w3-table-all w3-card-4">
                <tr>
                    <th>ID</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Actions</th>
                </tr>
                <% for(let element of dataUsers ) { %> <!-- dùng for of để render ra từng element trong mảng dataUsers -->
                    <tr>
                        <td><%=element.id %></td>
                        <td><%=element.firstName %></td>
                        <td><%=element.lastName %></td>
                        <td>
                            <button onclick="window.location.href='/detail/user/<%=element.id%>'">Detail</button>
                            <button onclick="window.location.href='/edit-user/<%=element.id%>'">Edit</button>
                            <button onclick="window.location.href='/delete-user/<%=element.id%>'">Detete</button>
                        </td>
                    </tr>
                <% } %>
            </table>
        </div> 
    
        <div class="create-user-container">
            <form action="/create-new-user" method="POST"> <!-- action được xử lý bên file web.js -->
                <h3>Create a new user</h3>
                <div>
                    <label>First name:</label>
                    <input type="text" name="firstName">
                </div>
                <div>
                    <label>Last name:</label>
                    <input type="text" name="lastName">
                </div>
                <div>
                    <label>Email:</label>
                    <input type="email" name="email">
                </div>
                <div>
                    <label>Address:</label>
                    <input type="text" name="address">
                </div>
                <div>
                    <button class="btn-submit" type="submit">Create</button>
                </div>
            </form>
        </div> 
    </div>
</body>
</html>
